import styled from "styled-components";

export const Container = styled.div`
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: -101;
  display: flex;
`;

export const IMG = styled.img`
  height: 100%;
  width: auto;
  z-index: -100;
`;
